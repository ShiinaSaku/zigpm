{
  "name": "@zigpm/zig",
  "version": "0.0.1",
  "private": true,
  "description": "Modern Zig distribution for npm — automatically packages every official Zig release",
  "keywords": [
    "compiler",
    "zig",
    "ziglang",
    "zigpm"
  ],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "publish": "bun zigpm.ts",
    "publish:dry": "bun zigpm.ts --dry-run",
    "build": "bun zigpm.ts --dry-run"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
